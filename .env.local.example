# Local development environment configuration
# Copy this file to .env.local and adjust values as needed

# Cluster Name
# Used for both kubectl context and GitOps paths
# Common values: docker-desktop, rancher-desktop, minikube, kind-kind
CLUSTER_NAME=rancher-desktop

# DNS Configuration
# External-DNS can create real DNS records from local cluster
# Leave empty or commented out to skip DNS record creation
DNS_ZONE=openportal.dev
DNS_PROVIDER=cloudflare

# Cloudflare Configuration (optional)

# CLOUDFLARE_USER_API_TOKEN=your-api-token-here
# CLOUDFLARE_ZONE_ID=your-zone-id-here
# CLOUDFLARE_ACCOUNT_ID=your-account-id-here