CERTIFICATE AUTHENTICATION - VISUAL GUIDE
==========================================

THE 4 KEY FILES
---------------

┌─────────────────────────────────────────────────────────────┐
│  1. alice.key (User's Private Key) 🔐 SECRET!              │
├─────────────────────────────────────────────────────────────┤
│  Where: ~/.kube/test-users/certs/alice.key                 │
│  Who has it: ONLY alice                                     │
│  What it is: Random secret key (like a password)           │
│  Purpose: Proves alice owns the certificate                │
│  Keep secret: YES! Anyone with this can impersonate alice  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  2. alice.crt (User's Certificate) 📄 PUBLIC                │
├─────────────────────────────────────────────────────────────┤
│  Where: ~/.kube/test-users/certs/alice.crt                 │
│  Who has it: alice (shown to Kubernetes)                   │
│  What it is: Identity card with username and groups        │
│  Contains:                                                  │
│    • Username: CN=alice                                     │
│    • Groups: O=developers, O=backend-team                   │
│    • Signature from Kubernetes CA                           │
│    • Expiration date                                        │
│  Keep secret: No (but only works with alice.key)           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  3. ca.crt (Kubernetes CA Certificate) 📋 PUBLIC            │
├─────────────────────────────────────────────────────────────┤
│  Where: /etc/kubernetes/pki/ca.crt (on master)             │
│         ~/.kube/test-users/certs/ca.crt (copy for users)   │
│  Who has it: Everyone (it's public)                         │
│  What it is: The "government" certificate                   │
│  Purpose: Kubernetes uses this to verify user certs        │
│  Keep secret: No (everyone needs it)                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  4. ca.key (Kubernetes CA Private Key) 🔐 CRITICAL SECRET!  │
├─────────────────────────────────────────────────────────────┤
│  Where: /etc/kubernetes/pki/ca.key (ONLY on master)        │
│  Who has it: ONLY Kubernetes control plane                 │
│  What it is: Master key that signs all certificates        │
│  Purpose: Sign user certificates to make them trusted      │
│  Keep secret: ABSOLUTELY! If leaked = full cluster breach  │
│  Users touch this: NEVER! (we use CSR API instead)         │
└─────────────────────────────────────────────────────────────┘


THE AUTHENTICATION FLOW
-----------------------

Step 1: alice wants to run kubectl
───────────────────────────────────

  alice's laptop:
  ┌──────────────────┐   ┌──────────────────┐
  │   alice.crt      │ + │   alice.key      │
  │  (certificate)   │   │  (private key)   │
  └────────┬─────────┘   └────────┬─────────┘
           │                      │
           └──────────┬───────────┘
                      │
                      ▼
              kubectl get pods
                (with cert auth)


Step 2: Kubernetes API Server validates
────────────────────────────────────────

  ┌────────────────────────────────────────────────┐
  │  Kubernetes API Server                         │
  │                                                │
  │  Receives:                                     │
  │  • alice.crt (certificate)                     │
  │  • Proof of alice.key ownership (signature)    │
  │                                                │
  │  Validation:                                   │
  │  ┌──────────────────────────────────────────┐ │
  │  │ 1. Check signature with ca.crt           │ │
  │  │    ✓ Certificate signed by trusted CA?   │ │
  │  └──────────────────────────────────────────┘ │
  │                ↓                               │
  │  ┌──────────────────────────────────────────┐ │
  │  │ 2. Check expiration                      │ │
  │  │    ✓ Certificate not expired?            │ │
  │  └──────────────────────────────────────────┘ │
  │                ↓                               │
  │  ┌──────────────────────────────────────────┐ │
  │  │ 3. Extract identity                      │ │
  │  │    Username: CN = alice                  │ │
  │  │    Groups: O = [developers, backend-team]│ │
  │  └──────────────────────────────────────────┘ │
  │                ↓                               │
  │  ┌──────────────────────────────────────────┐ │
  │  │ 4. Check RBAC                            │ │
  │  │    Does alice or her groups have         │ │
  │  │    permission to get pods?               │ │
  │  └──────────────────────────────────────────┘ │
  │                ↓                               │
  │           ALLOW / DENY                         │
  └────────────────────────────────────────────────┘


CERTIFICATE CREATION FLOW
-------------------------

What create-user-with-cert.sh does:

  Your Laptop                    Kubernetes Master
  ────────────                   ─────────────────

  1. Generate private key
     ┌──────────────┐
     │  alice.key   │ ← Stay local (secret)
     └──────┬───────┘
            │
            ▼
  2. Create CSR
     ┌──────────────┐
     │  alice.csr   │
     │ CN=alice     │
     │ O=developers │
     └──────┬───────┘
            │
            │ Submit via kubectl
            │
            ▼
                                  3. Receive CSR
                                     ┌─────────────────┐
                                     │ CSR resource    │
                                     │ (Kubernetes API)│
                                     └────────┬────────┘
                                              │
                                  4. Admin approves
                                     kubectl certificate
                                     approve alice-csr
                                              │
                                              ▼
                                     ┌─────────────────┐
                                     │   Sign with     │
                                     │   ca.key        │
                                     │   (automatic)   │
                                     └────────┬────────┘
                                              │
            ┌─────────────────────────────────┘
            │ Download signed cert
            ▼
     ┌──────────────┐
     │  alice.crt   │ ← Signed certificate!
     │ (signed)     │
     └──────────────┘

  5. Create kubeconfig
     ┌────────────────────────────┐
     │  alice-cert-kubeconfig.yaml│
     │                            │
     │  Contains:                 │
     │  • alice.crt (embedded)    │
     │  • alice.key (embedded)    │
     │  • ca.crt (embedded)       │
     └────────────────────────────┘

  6. Use kubectl
     export KUBECONFIG=alice-cert-kubeconfig.yaml
     kubectl get pods ✓


REAL-WORLD ANALOGY
------------------

Certificate Authentication = Government ID Card

┌────────────────────────────────────────────────────────┐
│  Private Key (alice.key)                              │
│  ═══════════════════════════════════════              │
│  Like: Secret key to access your ID card reader       │
│  Only you have it                                     │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  Certificate (alice.crt)                              │
│  ═══════════════════════════════════════              │
│  Like: Physical ID card with your photo and info     │
│  Shows to others, but only works with your key       │
│                                                       │
│  ID Card shows:                                       │
│  • Name: Alice                                        │
│  • Organization: Developers                           │
│  • Organization: Backend Team                         │
│  • Issued by: Kubernetes CA                           │
│  • Expires: 2026-10-31                                │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  CA Certificate (ca.crt)                              │
│  ═══════════════════════════════════════              │
│  Like: Government seal/watermark                      │
│  Everyone knows what it looks like                    │
│  Used to verify ID cards are real                     │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  CA Private Key (ca.key)                              │
│  ═══════════════════════════════════════              │
│  Like: Government's ID card printing machine          │
│  Only government has it                               │
│  If stolen: can print fake IDs for anyone!            │
└────────────────────────────────────────────────────────┘


SECURITY COMPARISON
-------------------

If compromised:

alice.key leaked:
  ✗ Attacker can impersonate alice
  ✗ Gets alice's permissions
  ✓ Solution: Delete alice's cert, create new one
  ⚠️ Impact: One user compromised

ca.key leaked:
  ✗ Attacker can create certs for ANY user
  ✗ Attacker can grant ANY groups
  ✗ Full cluster compromise
  ✓ Solution: Rotate entire CA (disaster recovery)
  ⚠️ Impact: ENTIRE CLUSTER compromised


STORAGE LOCATIONS
-----------------

Your Machine (~/.kube/test-users/):
  certs/
  ├── alice.key    🔐 SECRET - chmod 600
  ├── alice.crt    📄 Public - chmod 644
  ├── bob.key      🔐 SECRET - chmod 600
  ├── bob.crt      📄 Public - chmod 644
  └── ca.crt       📋 Public - chmod 644 (shared)

  alice-cert-kubeconfig.yaml  🔐 SECRET (contains alice.key)
  bob-cert-kubeconfig.yaml    🔐 SECRET (contains bob.key)

Kubernetes Master (/etc/kubernetes/pki/):
  ├── ca.crt       📋 Public
  ├── ca.key       🔐 CRITICAL SECRET - Never touch!
  ├── apiserver.crt
  ├── apiserver.key
  └── ... (other cluster certificates)


DO I NEED TO STORE/BACKUP?
---------------------------

alice.key:
  ✓ YES - Keep backup (encrypted!)
  ✓ Without it, certificate is useless
  ✓ If lost, create new certificate

alice.crt:
  ⚠️ Optional - Can recreate if you have CSR
  ✓ Easy to backup (it's small)
  ⚠️ If lost but have .key, create new cert

ca.crt:
  ⚠️ Optional - Can get from cluster anytime
  ✓ kubectl config view --raw
  ✓ Doesn't change often

ca.key:
  ✗ NEVER - You don't have it!
  ✗ Only on Kubernetes masters
  ✗ Managed by Kubernetes

Kubeconfig:
  ✓ YES - This has alice.key embedded!
  ✓ Encrypted backup recommended
  ✓ Treat like a password


CERTIFICATE LIFETIME
--------------------

Timeline:
                                            Expires ↓
Day 1 ────────── Day 180 ────────── Day 365 ┼ Day 366
  │                                          │    │
Create cert                                  │    Certificate
Still valid ✓                                │    expired ✗
                                             │
                                    Warning: expiring soon


Certificate expires after 1 year (configurable)

Renewal:
  1. Create new certificate (same username)
  2. Update kubeconfig
  3. Old cert stops working (expired)
  4. No RBAC changes needed (same username/groups)

Auto-renewal:
  ✗ Not automatic (unlike OIDC tokens)
  ✓ Need to manually recreate
  ✓ Or use cert-manager for automation


KEY POINTS
----------

1. Private key (alice.key) = Your password
   Keep it secret!

2. Certificate (alice.crt) = Your ID card
   Contains username + groups
   Only works with your private key

3. CA cert (ca.crt) = Government seal
   Public, everyone has it
   Used to verify certificates

4. CA key (ca.key) = ID printing machine
   Never touch it!
   Only Kubernetes has it

5. Our script uses CSR API
   ✓ Secure - no direct ca.key access
   ✓ Audited - approval logged
   ✓ RBAC controlled - only admins can approve

6. Store privately
   ✓ Encrypt backups
   ✓ Restrict file permissions (chmod 600)
   ✓ Don't commit to git
   ✓ Don't share in chat/email
